#include <bits/stdc++.h>

using namespace std;

vector<bool> seq;

int main() {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	
	int L, N, i, j;
	bool flag = true, x, y;
	cin >> L;
	seq.assign(L, 0);
	cin >> N;
	for (int k = 0; k < N; ++k) {
		cin >> i >> x >> j >> y;
	}

	cout << '\n';

	return 0;
}
